# Locus v3.5.2 Release Notes

**Release focus:** Codex doctor ownership hotfix.

`v3.5.2` fixes `locus-memory doctor codex` for real package-owned Codex MCP installs.

## Fixed

- `doctor codex` now falls back to `codex mcp get locus` when no test reader is injected.
- Real package-owned installs now report `Ownership: package-owned` instead of `missing`.

## Notes

- This hotfix builds on `v3.5.1`, which added safe recurring MCP `cwd = "$CODEX_HOME"` startup behavior.
- The final public Track B GitHub release story is documented in [v3.5.3](C:/Users/Admin/gemini-project/ClaudeMagnificoMem/docs/releases/v3.5.3.md).
